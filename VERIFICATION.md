# 验证记录

## 自动化验证

`python -m unittest -v`：32 项测试通过。

覆盖分页、增量重叠、去重、失败恢复、账号隔离、配置状态、直接 @／全员／未知分类、临时关注过期、手动禁用优先、ad-hoc 查询、中文搜索、定向刷新、上下文独立水位、媒体去重与解读缓存、分析批次以及跨平台任务定义。测试数据使用虚构身份，不包含真实消息。

附 GitHub Actions macOS／Windows 和 Python 3.10／3.13 矩阵；实际执行结果以仓库 Actions 为准。

## 本机集成验证

- macOS 上完成 CLI 隔离安装、缓存初始化、重复增量同步、本地查询和联系人发现验证。
- 媒体按 SHA-256 保存，重复请求返回缓存；未在仓库保留真实媒体。
- DWS @事件订阅成功建立并正常退出。观察期间没有新的真实 @事件，事件处理由确定性测试覆盖，尚未验证真实来信的完整链路。
- macOS LaunchAgent 安装、启动、状态查询、卸载和重新安装通过。
- 单次本地查询约 37 ms；一次空闲内存观察为 Python 约 28 MiB、DWS 消费者约 30 MiB、共享 bus 约 31 MiB。这些是单机观察，不是性能保证。
- Skill 格式校验通过。
- Windows 任务 XML 和参数生成有测试覆盖；Windows 原生 DWS、登录及后台调度仍需实机集成验证。

## 发布数据边界

仓库仅包含源代码、虚构测试数据、文档及 Skill。真实配置、聊天缓存、数据库、媒体、日志、虚拟环境和构建产物均排除于版本控制之外。
